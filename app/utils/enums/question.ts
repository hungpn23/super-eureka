export enum QuestionType {
  MULTIPLE_CHOICES = 'multiple choices',
  WRITTEN = 'written',
}

export enum QuestionDirection {
  TERM_TO_DEF = 'term to def',
  DEF_TO_TERM = 'def to term',
  BOTH = 'both',
}
